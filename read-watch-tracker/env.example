# sqlite（預設 Demo）
TRACKER_DB=sqlite
TRACKER_SQLITE_PATH=data/tracker.sqlite3

# PostgreSQL 正式 runtime
# TRACKER_DB=postgresql
# DATABASE_URL=postgresql://tracker:tracker@127.0.0.1:5432/read_watch_tracker
